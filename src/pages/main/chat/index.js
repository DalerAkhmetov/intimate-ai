import { gsap } from 'gsap';

const $section = document.querySelector('.main__section--chat');

const resize = () => {
    if (!$section) {
        return;
    }
};

const init = () => {
    if (!$section) {
        return;
    }
};

export default {
    init,
    resize,
};
